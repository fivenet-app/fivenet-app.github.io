---
title: Config File Reference
description: Quick reference for the most important FiveNet configuration options.
---

::callout{icon="i-mdi-information-outline"}
This page highlights the most important configuration options for getting FiveNet running. It is intentionally **not** a full list of every available setting.
::

## Config File Location

By default, FiveNet looks for `config.yaml` in this order:

1. `.` - The current working directory
2. `/config` - Commonly used by containerized deployments

::callout
---
color: secondary
icon: i-mdi-file-document-outline
to: https://github.com/fivenet-app/fivenet/blob/main/config.example.yaml
---
Start from the example configuration file to build your own `config.yaml`.
::

## Required Options

These are the settings you should treat as mandatory for a working installation.

::field{required name="http.publicURL" type="string"}
The public URL of your FiveNet instance, for example `https://fivenet.example.com`.
::

::field{required name="http.sessions.cookieSecret" type="string"}
Random secret used for session cookies. Generate it once and keep it stable. Use at least 24 characters.
::

::field{required name="http.sessions.domain" type="string"}
The public domain or subdomain FiveNet runs on, for example `fivenet.example.com`.
::

::field{required name="database.dsn" type="string"}
MySQL or MariaDB connection string.

```text
DB_USER:DB_PASS@tcp(DB_HOST:DB_PORT)/DB_NAME?collation=utf8mb4_unicode_ci&parseTime=True&loc=Europe%2FBerlin
```

Keep the parameters after `?`. In most cases, only the `loc=` timezone part should be changed.
::

::field{required name="nats.url" type="string"}
NATS connection URL, including credentials if needed, for example `nats://USER:PASSWORD@localhost:4222`.

JetStream must be enabled on the NATS server.
::

::field{required name="jwt.secret" type="string"}
Secret used to sign user tokens. Generate it once and keep it stable. Use at least 24 characters.
::

::field{name="auth.superuserGroups" type="string[]"}
Groups that should receive superuser access in FiveNet.
::

::field{name="sync.enabled + sync.apiTokens" type="boolean + string[]"}
Enable the Sync API and provide at least one token. This is required for the FiveNet plugin and DBSync.
::

## Commonly Adjusted Options

These options are not always required, but they are among the most common ones to change during setup.

::field{name="http.origins" type="string[]"}
Allowed origins for requests to FiveNet. In production, set this to the domains that should access your instance.
::

::field{name="http.trustedProxies" type="string[]"}
Trusted reverse proxy IPs or CIDR ranges. Set this when FiveNet runs behind NGINX, Traefik, or another proxy and should trust forwarded client IP headers.
::

::field{default="filesystem" name="storage.type" type="'filesystem' | 's3'"}
Choose whether uploads should be stored on the local filesystem or in S3-compatible object storage.
::

::field{name="database.esxCompat" type="boolean"}
Enable this only if FiveNet should read directly from your ESX database tables. Most setups should prefer DBSync instead.

See [ESX Compatibility Mode](2.esx-compat-mode.md) for details.
::

::field{name="oauth2.providers" type="array"}
Configure external login providers such as Discord or generic OpenID/OAuth2 providers.
::

::field{name="tracing.enabled" type="boolean"}
Enable OpenTelemetry-based tracing when you want deeper monitoring or troubleshooting.
::

## Minimal Config Skeleton

This is a practical starting point for a basic installation:

```yaml
http:
  publicURL: "https://fivenet.example.com"
  origins:
    - "https://fivenet.example.com"
  sessions:
    cookieSecret: "change-this-to-a-random-secret"
    domain: "fivenet.example.com"

database:
  dsn: "DB_USER:DB_PASS@tcp(DB_HOST:3306)/DB_NAME?collation=utf8mb4_unicode_ci&parseTime=True&loc=Europe%2FBerlin"

nats:
  url: "nats://USER:PASSWORD@localhost:4222"

jwt:
  secret: "change-this-to-another-random-secret"

auth:
  superuserGroups:
    - "admin"

sync:
  enabled: true
  apiTokens:
    - "change-this-to-a-random-sync-token"
```

## Notes

- Keep `cookieSecret`, `jwt.secret`, and sync tokens stable after deployment unless you intentionally want to invalidate existing sessions or access.
- Prefer DBSync over `database.esxCompat` unless you specifically need direct ESX table compatibility.
- For the full set of available options, comments, and examples, use [`config.example.yaml`](https://github.com/fivenet-app/fivenet/blob/main/config.example.yaml) as the source of truth.
